<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.techstar.pvms.backend.module.forecast.repository.ForecastStationMapper">

    <select id="findAll" resultType="cn.techstar.pvms.backend.module.forecast.repository.ForecastStationMapper$StationRow">
        SELECT
            s.id,
            s.name,
            s.company_id,
            c.name AS company_name,
            ps.resource_unit_id,
            c.region,
            s.capacity_kwp,
            s.status,
            s.data_quality,
            s.sort_index
        FROM sa_station s
        JOIN sa_company c ON c.id = s.company_id
        LEFT JOIN pm_station ps ON ps.id = s.id
        ORDER BY c.sort_index, s.sort_index
    </select>

</mapper>
