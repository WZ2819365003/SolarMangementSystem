<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.techstar.pvms.backend.module.productionmonitor.repository.ProductionMonitorResourceUnitMapper">

    <select id="findAll" resultType="cn.techstar.pvms.backend.module.productionmonitor.repository.ProductionMonitorResourceUnitMapper$ResourceUnitRow">
        SELECT
            id,
            name,
            region,
            city,
            status,
            cluster_radius_km,
            dispatch_mode,
            strategy_label,
            latest_alarm_title,
            latest_alarm_time,
            alarm_total,
            alarm_critical,
            alarm_major,
            alarm_minor
        FROM pm_resource_unit
        ORDER BY id
    </select>

</mapper>
