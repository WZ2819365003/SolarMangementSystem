<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.techstar.pvms.backend.module.strategy.repository.StrategyPriceMapper">

<select id="findByStationId" resultType="cn.techstar.pvms.backend.module.strategy.repository.StrategyPriceMapper$PricePeriodRow">
        SELECT station_id, period_order, start_slot, end_slot, price_type, price_cny_per_kwh
        FROM sg_price_period
        WHERE station_id = #{stationId}
        ORDER BY period_order
    </select>

    <select id="findTemplate" resultType="cn.techstar.pvms.backend.module.strategy.repository.StrategyPriceMapper$PricePeriodRow">
        SELECT station_id, period_order, start_slot, end_slot, price_type, price_cny_per_kwh
        FROM sg_price_period
        WHERE station_id = 'SZ-001'
        ORDER BY period_order
    </select>

</mapper>
