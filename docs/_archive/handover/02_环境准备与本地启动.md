# 环境准备与本地启动

## 1. 必备环境

本项目存在两套前端运行环境要求，不要混淆：

| 用途 | 版本要求 | 说明 |
| --- | --- | --- |
| 前端主应用运行/构建 | Node 14.16.0 + npm 6.14.11 | 见 `frontend/package.json` |
| Playwright 自动化 | Node 18+ | 见 `frontend/tests/playwright/package.json` |
| 后端运行 | Java 21 | 见 `backend/pom.xml` |
| 后端构建/测试 | Maven 3.x | 本地需可执行 `mvn` |
| 数据库 | MySQL 8.x | localhost:3306，库名 `pvms` |

## 2. 前端环境文件

前端示例环境文件位于：

- `frontend/.env.example`

当前示例字段：

```env
VUE_APP_AMAP_KEY=your_amap_key
VUE_APP_AMAP_SECURITY_CODE=your_amap_security_code
VUE_APP_AMAP_VERSION=2.0
```

如果高德地图相关页面报错，优先检查这几个变量是否配置。

## 3. 前端依赖注意事项

前端依赖里有一个需要特别注意的本地包：

- `hs-dtk-ui`: `file:../../vpp-main/node_modules/hs-dtk-ui/hs-dtk-ui-0.1.29.tgz`

这意味着：

- 这不是一个公共 npm 依赖
- 如果本机没有对应路径，`npm install` 会失败
- 新同事接手时，需要先确认本机是否具备 `vpp-main` 依赖目录，或者改成团队统一可访问的包源

## 4. 启动前端

在仓库根目录执行：

```powershell
cd frontend
C:\Users\zhuow\tools\node-v14.16.0-win-x64\npm.cmd install --scripts-prepend-node-path=true
C:\Users\zhuow\tools\node-v14.16.0-win-x64\npm.cmd run serve --scripts-prepend-node-path=true
```

默认端口：

- `http://127.0.0.1:6618`

开发服务器配置位于：

- `frontend/vue.config.js`

关键点：

- dev server 端口固定为 `6618`
- 本地开发 `publicPath` 为 `/`
- 生产构建 `publicPath` 为 `/pvms/`

## 5. 准备 MySQL

后端 v2.0.0 使用 MySQL 作为生产数据库（测试仍用 H2 内存库）。

```bash
# 建库
mysql -u root -p123456 -e "CREATE DATABASE IF NOT EXISTS pvms CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 建表 + 灌数据（注意指定 utf8mb4）
mysql -u root -p123456 --default-character-set=utf8mb4 pvms < backend/src/main/resources/schema.sql
mysql -u root -p123456 --default-character-set=utf8mb4 pvms < backend/src/main/resources/mysql-data.sql
```

连接信息在 `backend/src/main/resources/application.yml`：

- URL：`jdbc:mysql://localhost:3306/pvms`
- 用户名：`root`
- 密码：`123456`

## 6. 启动后端

```bash
cd backend
mvn test              # 49 项测试（H2 内存库）
mvn spring-boot:run   # 连接 MySQL，端口 8091
```

默认端口：

- `http://127.0.0.1:8091`

基础健康检查：

- `GET http://127.0.0.1:8091/api/system/health`

## 7. 启动顺序建议

建议按下面顺序操作：

1. 先起后端，确认 `health` 正常
2. 再起前端
3. 打开前端页面确认菜单和路由正常
4. 如需跑 Playwright，再单独进入 `frontend/tests/playwright`

## 8. 运行 Playwright

Playwright 目录和主前端目录的 Node 版本要求不同，请单独处理。

```powershell
cd frontend/tests/playwright
npm install
npm run install:browsers
npm test
```

Playwright 配置文件：

- `frontend/tests/playwright/playwright.config.js`

当前配置特点：

- 自动拉起前端开发服务器
- 默认访问 `http://127.0.0.1:6618/dashboard/overview`
- 失败时保留 trace 和截图
- HTML 报告输出到 `frontend/tests/playwright/output/report`

## 9. 本地启动后的验证动作

前端启动后，建议手工访问以下页面确认路由都可打开：

- `/dashboard/overview`
- `/production-monitor/overview`
- `/forecast/overview`
- `/strategy/list`
- `/devices/monitor`
- `/alarms/center`

后端启动后，建议至少验证：

- `/api/system/health`
- `/api/pvms/dashboard/overview`
- `/api/pvms/forecast/meta`
- `/api/pvms/strategy/meta`

## 10. 常见启动问题

### `npm install` 失败

优先排查：

- Node 版本是否为 14.16.0
- `hs-dtk-ui` 本地 tgz 路径是否存在

### 地图相关页面报错

优先排查：

- `.env` 中的高德 key/security code 是否配置

### Playwright 起不来

优先排查：

- 是否使用了 Node 18+
- 浏览器是否执行过 `npm run install:browsers`

### 后端连接 MySQL 失败

优先排查：

- MySQL 是否在运行（`mysql -u root -p123456 -e "SELECT 1"`）
- `pvms` 库是否已创建
- 是否已执行 `schema.sql` 建表

### 后端能起但返回空数据

优先排查：

- 是否已灌入 `mysql-data.sql` 种子数据
- 连接字符集是否为 `utf8mb4`（否则中文会报错）

